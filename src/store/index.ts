import { createStore } from 'vuex'
import medico from './modules/medico'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    medico
  }
})
